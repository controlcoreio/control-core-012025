FROM permitio/opal-server:latest

LABEL org.opencontainers.image.title="ControlCore Policy Admin - Server"
LABEL org.opencontainers.image.description="Fine-grained policy agent server for ControlCore.io"
LABEL org.opencontainers.image.authors="ControlCore.io"
LABEL org.opencontainers.image.vendor="ControlCore"
LABEL org.opencontainers.image.licenses="Apache-2.0"
LABEL org.opencontainers.image.source="https://controlcore.io"

# (Optional) Add branding files (e.g., logos, config, README)
# COPY ./branding/ /app/branding/
# Example: COPY ./README.ControlCore.md /app/README.md

# (Optional) Change default config if needed
# COPY ./controlcore-opal-config.yaml /app/opal-config.yaml

# Ensure original LICENSE is included (required by Apache 2.0)
COPY LICENSE /LICENSE