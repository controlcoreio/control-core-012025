{{- if .Values.bouncer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.bouncer.name }}-hpa
  labels:
    app: {{ .Values.bouncer.name }}
    {{- include "controlcore.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.bouncer.name }}
  minReplicas: {{ .Values.bouncer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.bouncer.autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.bouncer.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.bouncer.autoscaling.targetMemoryUtilizationPercentage }}
  behavior:
    {{- toYaml .Values.bouncer.autoscaling.behavior | nindent 4 }}
{{- end }}
