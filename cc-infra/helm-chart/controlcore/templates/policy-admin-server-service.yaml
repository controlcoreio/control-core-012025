apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.policyAdminServer.name }}
  labels:
    app: {{ .Values.policyAdminServer.name }}
    {{- include "controlcore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.policyAdminServer.service.type }}
  ports:
    - port: {{ .Values.policyAdminServer.service.port }}
      targetPort: {{ .Values.policyAdminServer.service.port }}
      protocol: TCP
  selector:
    app: {{ .Values.policyAdminServer.name }}
    stack: {{ .Values.global.stack }}
    env: {{ .Values.global.environment }}